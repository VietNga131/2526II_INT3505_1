server {
    listen 80;
    
    location / {
        # Lớp 1 (Nginx) chuyển tiếp tới Lớp 2 (Gunicorn/Flask)
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}